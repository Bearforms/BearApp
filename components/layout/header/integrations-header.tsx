'use client';

import { memo } from 'react';
import { BaseHeader } from './base-header';

export const IntegrationsHeader = memo(function IntegrationsHeader() {
  return <BaseHeader title="Integrations" />;
});