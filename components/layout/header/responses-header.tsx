'use client';

import { memo } from 'react';
import { BaseHeader } from './base-header';

export const ResponsesHeader = memo(function ResponsesHeader() {
  return <BaseHeader title="Form Responses" />;
});