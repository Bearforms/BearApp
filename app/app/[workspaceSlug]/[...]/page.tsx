"use client"

interface Props {}

const page = ({} : Props ) => {
	return (
		<main className="flex min-h-screen flex-col flex-1 w-full items-center justify-center">
			<h1 className="">
				Page not found
			</h1>
			<p>
				Please go back to the previous page
			</p>
		</main>
	);
};

export default page;
